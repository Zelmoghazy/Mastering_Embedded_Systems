#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a;
    float b;
    printf("Enter two numbers: ");
    scanf("%f %f",&a,&b);
    printf("Product: %f\n",a*b);
    system("pause");
    return 0;
}